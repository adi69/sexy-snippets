`makemigrations` will make migration queries for the **internal django tables** only
`makemigrations app-name` will make migration queries for the specific apps

so first makemigrations + makemigrations <each-app>
then, migrate
